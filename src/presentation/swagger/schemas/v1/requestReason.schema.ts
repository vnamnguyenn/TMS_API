/**
 * @swagger
 * components:
 *   schemas:
 *     RequestReason:
 *       type: object
 *       properties:
 *         id:
 *           type: string
 *           format: uuid
 *         name:
 *           type: string
 *         requestType:
 *           type: object
 *           properties:
 *             id:
 *              type: string
 *              format: uuid
 *             name:
 *              type: string
 *         createdDate:
 *           type: string
 *           format: date-time
 *         updatedDate:
 *           type: string
 *           format: date-time
 */
