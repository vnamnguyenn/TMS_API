/**
 * @swagger
 * components:
 *   schemas:
 *     User:
 *       type: object
 *       properties:
 *         id:
 *           type: string
 *           format: uuid
 *         userName:
 *           type: string
 *         address:
 *           type: string
 *         birthday:
 *           type: string
 *         departmentId:
 *           type: string
 *           format: uuid
 *         email:
 *           type: string
 *           format: email
 *         phoneNumber:
 *           type: string
 *         specificationId:
 *           type: string
 *           format: uuid
 *         supervisor:
 *           type: string
 *           format: uuid
 *         userCode:
 *           type: string
 *         createdDate:
 *           type: string
 *           format: date-time
 *         updatedDate:
 *           type: string
 *           format: date-time
 */
